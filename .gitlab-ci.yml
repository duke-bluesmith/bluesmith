# Select what we should cache
cache:
  paths:
  - vendor/

before_script:
# Sanity checks
- hostname
- whoami
- pwd

# Install composer dependencies
- composer install

services:
- mysql

test:app:
  script:
  - vendor/bin/phpunit
